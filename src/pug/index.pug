<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="css/styles.css")
        title Mercaux
    body
        
        ol.main-menu
            -for (var i = 1; i <= 12; i++) {
                li
                    a(href= `task_${i}.html` target="_blank")= `Task ${i}`
            -}